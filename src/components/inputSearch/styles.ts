import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginHorizontal: 10,
    backgroundColor: '#fff',
    paddingHorizontal: 10,
    borderRadius: 10,
  },
  btnClose: {
    width: 25,
    marginRight: 5,
  },
  inputContainer: {
    flex: 1,
    fontSize: 14,
    fontWeight: 'bold',
  },
});
